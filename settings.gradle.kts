pluginManagement {
    repositories {
        google()
        jcenter()
        gradlePluginPortal()
        mavenCentral()
    }
}
//includeBuild("convention-plugins")
rootProject.name = "di-multiplatform-lib"

